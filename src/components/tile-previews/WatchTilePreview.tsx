"use client";

/** Minimal preview for Video tile when sidebar is shown (multi-track). */
export function WatchTilePreview() {
  return (
    <div className="flex h-14 items-center justify-center rounded-lg bg-black/[0.04] text-[11px] text-black/40 dark:bg-white/[0.06] dark:text-white/40">
      Video explore
    </div>
  );
}
